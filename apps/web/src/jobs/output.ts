/**
 * Use this function when you really need to log to the server log as a feature.
 * So we can easily find if it is useful or a remaining debug console.log
 */
// eslint-disable-next-line no-console
export const output = console
