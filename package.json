{
  "name": "my-app",
  "description": "My App - Monorepo",
  "version": "0.1.0",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "engines": {
    "node": "^24.5"
  },
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "build": "turbo build",
    "cli": "dotenv -- ./apps/cli/node_modules/.bin/tsx apps/cli/src/index.ts",
    "clean": "rm -rf node_modules",
    "clean:workspaces": "turbo clean",
    "dev": "turbo dev --parallel",
    "format": "prettier --write \"**/*.{ts,tsx,md,css}\"",
    "lint": "turbo lint",
    "tsc": "turbo tsc",
    "test": "turbo test",
    "test:integration": "pnpm -F web test:integration",
    "test:e2e": "pnpm -F e2e cy open",
    "with-env": "dotenv --",
    "scw": "pnpm with-env scw",
    "manypkg": "manypkg",
    "start:web": "pnpm -F web dev",
    "fixtures:load": "pnpm -F fixtures load",
    "prisma:generate-migration": "pnpm --silent -F @app/web prisma migrate dev --name",
    "db:init": "pnpm -F web prisma generate && pnpm -F web prisma migrate deploy",
    "docker:start": "docker compose -f ./docker-compose.dev.yml -p stack up -d",
    "docker:stop": "docker compose -f ./docker-compose.dev.yml -p stack down",
    "docker:reset": "pnpm docker:stop && docker volume rm stack_pgdata && pnpm docker:start && pnpm db:init",
    "coverage:merge": "rm -rf coverage/.nyc_output && mkdir -p coverage/.nyc_output && find coverage-reports -name '*.json' -exec cp {} coverage/.nyc_output/ \\; && nyc report --temp-dir ./coverage/.nyc_output --reporter=lcov --reporter=html --reporter=text-summary --report-dir=./coverage",
    "coverage:upload": "cat ./coverage/lcov.info | coveralls"
  },
  "pnpm": {
    "overrides": {}
  },
  "optionalDependencies": {
    "turbo-linux-64": "^2.5.3",
    "turbo-linux-arm64": "^2.5.3"
  },
  "volta": {
    "node": "24.5.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.3",
    "@manypkg/cli": "^0.24.0",
    "coveralls": "^3.1.1",
    "dotenv": "^16.5.0",
    "dotenv-cli": "^8.0.0",
    "nyc": "^17.1.0",
    "ts-node": "^10.9.2",
    "turbo": "^2.5.3"
  }
}
