version: '3.8'

services:
  db:
    image: postgres:12.14-alpine
    container_name: la-base_pg
    environment:
      - POSTGRES_DB=la-base
      - POSTGRES_USER=la-base
      - POSTGRES_PASSWORD=password
    ports:
      - '5433:5432'
    volumes:
      - la-base-pgdata:/var/lib/postgresql/data
    networks:
      - la-base-network
    restart: always

volumes:
  la-base-pgdata:
    name: la-base_pgdata

networks:
  la-base-network:
    external: false
    name: la-base-network

